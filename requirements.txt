## 📦 **requirements.txt**

Include all the runtime dependencies your project actually uses — here’s a clean, minimal version:

```txt
# Core dependencies
requests==2.32.5
psycopg==3.2.12
dotenv==1.2.1

# Testing
pytest==9.0.0
